kind: pipeline
type: kubernetes
name: default

# trigger

#platform:
#  os: linux
#  arch: arm64

steps:
- name: hello
  image: busybox
  commands:
  - echo hello world
  
trigger:
  branch:
  - main
