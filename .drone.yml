kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64

steps:

- name: slack
  image: plugins/slack
  settings:
    webhook:
      from_secret: MY_SLACK_WEBHOOK

- name: hello
  image: busybox
  commands:
  - echo hello world
 
trigger:
  branch:
  - main
  
